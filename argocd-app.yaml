apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: ai-devsecops-backend
  namespace: argocd

spec:
  project: default

  source: 
    repoURL: https://github.com/gaurav-2004216/devops-ai-platform.git
    targetRevision: main
    path : kubernetes

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
